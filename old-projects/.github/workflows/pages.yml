name: Deploy GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: |
            vector-defence-classic-port/package-lock.json
            swarm-silverlight/package-lock.json

      - name: Install dependencies
        run: |
          npm ci --prefix vector-defence-classic-port
          npm ci --prefix swarm-silverlight

      - name: Build
        run: |
          npm run build --prefix vector-defence-classic-port
          npm run build --prefix swarm-silverlight

      - name: Assemble Pages artifact
        run: |
          mkdir -p pages/balls pages/click-me pages/js-fireworks pages/shared pages/shoot-them-down pages/spaceship pages/swarm pages/swarm-silverlight pages/vector-defence-classic-port
          cp index.html pages/index.html
          cp -R balls/. pages/balls/
          cp -R click-me/. pages/click-me/
          cp -R js-fireworks/. pages/js-fireworks/
          cp -R shared/. pages/shared/
          cp -R shoot-them-down/. pages/shoot-them-down/
          cp -R spaceship/. pages/spaceship/
          cp -R swarm/. pages/swarm/
          cp -R swarm-silverlight/dist/. pages/swarm-silverlight/
          cp -R vector-defence-classic-port/dist/. pages/vector-defence-classic-port/

      - name: Setup Pages
        uses: actions/configure-pages@v6
        with:
          enablement: true

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: pages

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
